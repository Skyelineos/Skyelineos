# Skyeline OS App Audit — 2026-05-28

User ask: "Go through the entire app and analyze for problems and incomplete buildouts. Make it better, simple, and look good."

This doc captures the audit findings + the prioritized fix list. Items get checked off as they ship.

## Top 10 highest-impact fixes (rank-ordered)

| # | Fix | Effort | Status |
|---|-----|--------|--------|
| 1 | Wire `/subcontractor-portal/tasks|invoices` + `/client-portal/invoices|schedule` link targets correctly. Every sub + client hits these "View all" links today and nothing happens. | S | ☐ |
| 2 | Compliance upload in `SubcontractorPortal.tsx:502` is a toast "coming soon" — subs can't actually submit W-9/COI/agreement. Either build or hide. | M | ☐ |
| 3 | `WeatherForecast` is hardcoded mock data (Jan 2024 dates). Replace with real API or remove. | S/M | ☐ |
| 4 | `/accounting` is referenced from `roleRedirects.ts:49` and `Dashboard.tsx:57` but no route exists. Latent admin 404. | S | ☐ |
| 5 | Financials → Reports tab body says "Financial reporting features coming soon..." Hide the tab or build. | S/L | ☐ |
| 6 | Empty states on Sales, SiteLog, DesignBoard, Reports have no CTA. Follow Projects.tsx/Contacts.tsx pattern. | S | ☐ |
| 7 | Native `confirm()` deletes throughout — replace with `<AlertDialog>` that names what will cascade. | M | ☐ |
| 8 | Brand gold `#C9A96E` hardcoded in 30+ files. Centralize as CSS var + Tailwind theme color. | L | ☐ |
| 9 | Role/portal terminology inconsistent: Sub vs Subcontractor, Owner vs Client vs Homeowner. | S | ☐ |
| 10 | Delete dead pages (`ProjectsOverview`, `ClientPortal`, `Accounting`, mock constants in `Dashboard.tsx`, `App-full-no-auth.tsx`). | S | ☐ |

## Honorable mentions

- `AIRenderingStudio` button opens a "Coming Soon" modal — hide or feature-flag.
- `PendingReviewsCard.tsx:119` — literal "(Google review link coming soon)" goes out in real client emails today.
- `useScheduleData.ts` has 6 unimplemented Firestore CRUD stubs — verify nothing depends on them.

## Broken routes / 404s waiting to happen

- `/accounting` — `Dashboard.tsx:57`, `roleRedirects.ts:49`
- `/client-portal/invoices` — `ClientTodayFeed.tsx:200, 213` (no `invoices` case in SkyelineClientPortal TABS — falls through to dashboard)
- `/client-portal/schedule` — `ClientTodayFeed.tsx:224, 232` (same)
- `/subcontractor-portal/tasks` — `SubTodayFeed.tsx:142, 164` (no `tasks` tab in SubcontractorPortal)
- `/designer-portal` (bare) and `/designer-portal/selections` — `DesignerTodayFeed.tsx:102+`
- `/projects/new`, `/projects/list` — `ProjectsOverview.tsx` (dead code; safe but should delete)

## Dead-end buttons / no-op CTAs

- `SelectionsManager.tsx:408` — "Save to Catalog too" → `onClick={() => {}}`
- `SubcontractorPortal.tsx:502` — Compliance "Upload" → toast "Upload coming soon"
- `DocumentTemplateEditor.tsx:182` — "Download PDF" → toast "PDF export coming soon"

## Visible "Coming Soon" placeholders

- `Financials.tsx:305` — Reports tab body
- `ClientPortal.tsx:436` — "Design selection interface coming soon..." (may be dead code; SkyelineClientPortal is active)
- `UltimateTimelineBuilder.tsx:541` — "Advanced Analytics Coming Soon"
- `DependencyManager.tsx:522` — "Interactive network diagram coming soon..."
- `DhtmlxScheduleSection.tsx:244` — "Calendar View - Coming Soon"
- `AIRenderingStudio.tsx:42` — entire feature is a Coming Soon modal
- `Tools.tsx:139` + `ImportCenter.tsx:1488` — disabled tools (labeled, less harmful)
- `PendingReviewsCard.tsx:119` — emailed to clients today

## Empty states missing CTAs

- `Sales.tsx:266`, `SiteLog.tsx:70`, `DesignBoard.tsx:194`, `Reports.tsx:170`
- `SubcontractorPortal.tsx:308, 349, 386, 524` — sub portal empty states
- `SkyelineClientPortal.tsx:132` — "no projects assigned" with no contact-your-GC affordance

## Outright broken features

- `WeatherForecast.tsx` — hardcoded mock data
- `Dashboard.tsx:19-43` — `upcomingSchedule`, `alertsAndDecisions`, `financialSummary` mock constants (imported, never rendered, but ripe for accidental future use)
- `projectUtils.ts:103` — project progress calc TODO; progress bars across app are fake
- `useScheduleData.ts:48-212` — 6 unimplemented Firestore CRUD TODOs
- `ScheduleEditorModal.tsx:165` — hardcoded `createdBy: 1` (legacy SQL id) instead of auth UID

## Inconsistent styling

- Brand gold `#C9A96E` inline in 30+ files; competing shades `#8a6a3a`, `#8B6F3F`, `rgba(201,169,110,...)`. CSS var `--accent-color` exists but only used in 2 files.
- Heavy inline `style={{}}` in Sales, LumberTakeoff, Tools.
- Native `confirm()` deletes mixed with shadcn `<AlertDialog>` — visual whiplash for the same destructive action.

## Heavy UI / performance

- No pagination anywhere; all list pages render full array via `.map`.
- `recharts`, `jspdf`, `html2canvas`, `pdfmake` in financial routes — not split, loads on any financial page mount.
- `SubcontractorPortal.tsx:159-207` opens 5 simultaneous `onSnapshot` queries on mount.
- `SkyelineClientPortal.tsx:89-119` runs 2 unbounded queries.
- No skeleton loaders — pages flash spinner then snap.
- `SubcontractorPortal` re-derives stats every render without `useMemo`.

# GitHub sync guard — informational warning before pushing. Non-blocking:
# it prints whether you're behind origin/main so you don't push/deploy stale
# work, but never blocks the push itself.
sh scripts/git-sync-check.sh || true
